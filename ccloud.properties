# Kafka configuration for local development
bootstrap.servers=localhost:9092

# Schema Registry (not needed for this app but included for completeness)
schema.registry.url=http://localhost:8081

# Security settings (not needed for local)
security.protocol=PLAINTEXT

# Consumer settings
consumer.group.id=binance-filtering-group